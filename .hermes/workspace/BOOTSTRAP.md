# BOOTSTRAP.md - First NemoClaw Hermes VSS Run

1. Read `ENV.md` and `TOOLS.md`.
2. Export the variables from `ENV.md`.
3. Confirm the `vss_orchestrator` MCP server is available in the Hermes tool list.

If this cannot connect, tell the user to start the VSS Orchestrator MCP server
from `deploy/docker/scripts/deploy_nemoclaw_vss.ipynb` before asking you to
deploy VSS.

After the first successful session, this file can be ignored.
